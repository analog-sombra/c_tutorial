#include<stdio.h>
int main(int argc, char const *argv[])
{
    // - register keyword hints the compiler to store a variable in register memory.
    // - This is done because access time reduces greatly for most frequently referred variable
    // - This is the choice of vompiler whether it puts the given variable in register or not.
    // - usually compiler themselves do the necessary
    return 0;
}
